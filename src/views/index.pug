doctype html
html(lang='en')
  head
    title MEAN Web App
    link(href='lib/bootstrap/dist/css/bootstrap.min.css', rel='stylesheet')
    link(href='css/styles.css', rel='stylesheet')
    script(src='lib/jquery/dist/jquery.min.js')
    script(src='lib/bootstrap/dist/js/bootstrap.min.js')
  body
    header.navbar.navbar-bright.navbar-fixed-top(role='banner')
      .container
        .navbar-header
          button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
            span.icon-bar
            span.icon-bar
            span.icon-bar
          a.navbar-brand(href='/') Home
        nav.collapse.navbar-collapse(role='navigation')
          ul.nav.navbar-nav
              each item in nav
                li
                    a(href=item.link)= item.text
          ul.nav.navbar-right.navbar-nav
            li.dropdown
              a.dropdown-toggle(href='#', data-toggle='dropdown')
                i.glyphicon.glyphicon-search
              ul.dropdown-menu(style='padding:12px;')
                form.form-inline
                  button.btn.btn-default.pull-right(type='submit')
                    i.glyphicon.glyphicon-search
                  input.form-control.pull-left(type='text', placeholder='Search')
    #masthead
      .container
        | Welcome to MEAN Web App !!
